[package]
name = "http"
version = "0.1.0"
edition = "2021"

[dependencies]
rusty-js = { workspace = true }
tokio = { version = "1.0", features = ["rt", "time", "macros"] }
http_crate = { package = "http", version = "1.2" }

[dev-dependencies]
rustyjs_test = { path = "../../utils/rustyjs_test" }
