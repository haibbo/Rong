# Navigator

Global `navigator` object providing runtime environment info.

```javascript
navigator.userAgent;  // User-Agent string
navigator.platform;   // platform identifier (e.g. "macos", "linux", "windows")
navigator.arch;       // CPU architecture
```
