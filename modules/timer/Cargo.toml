[package]
name = "timer"
version = "0.1.0"
edition = "2021"

[dependencies]
rong = { workspace = true }
tokio = { version = "1.0", features = ["rt", "time", "macros"] }
futures = "0.3"

[dev-dependencies]
rong_test = { path = "../../utils/rong_test" }
console = { path = "../console" }
assert = { path = "../assert" }
