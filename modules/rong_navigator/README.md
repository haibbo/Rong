# rong_navigator

Exposes system information through the global `navigator` object.

## JS APIs

- `navigator.userAgent` — user agent string
- `navigator.platform` — platform identifier (e.g., "macos", "linux", "windows")
- `navigator.arch` — CPU architecture (e.g., "x86_64", "aarch64")
